void funccom() {
  vidas();
  tone(3, 349, 149); //F4
  chuva(2, 9, 7, 149); //ISSO SUBSTITUI O DELAY I N T E I R O QUE TAVA AQUI ANTES, FAZER OS LEDS DE 2 EM 2
  tone(3, 415, 149); //Ab/G#4
  chuva(2, 7, 5, 149);
  tone(3, 466, 149); //Bb/A#4
  chuva(2, 5, 2, 149);
  tone(3, 466, 446); //Bb/A#4
  chuva(2, 2, 0, 149); //O PADRAO VAI SER 149 FODASE, SE FOR MAIOR QUE 149, DIMINUI 149 E O RESULTADO FICA COMO DELAY ALI EM BAIXO
  delay(297);
  tone(3, 415, 297); //Ab/G#4
  chuva(1, 9, 0, 745);
  delay(740);
  tone(3, 349, 149); //F4
  chuva(2, 9, 7, 149);
  tone(3, 415, 149); //Ab/G#4
  chuva(2, 7, 5, 149);
  tone(3, 466, 149); //Bb/A#4
  chuva(2, 5, 2, 149);
  tone(3, 466, 446); //Bb/A#4
  chuva(2, 2, 0, 298);
  tone(3, 415, 298); //Ab/G#4
  chuva(0, 9, 5, 298);
  tone(3, 349, 298); //F4
  chuva(0, 5, 2, 298);
  tone(3, 311, 149); //Eb/D#4
  chuva(0, 2, 0, 149);
  tone(3, 349, 149); //F4
  Serial.print("Score: ");
  Serial.println(score);
  delay(595);
  tone(3, 466, 149); //Bb/A#4
  chuva(1, 9, 7, 149);
  tone(3, 415, 149); //Ab/G#4
  chuva(1, 7, 5, 149);
  tone(3, 349, 149); //F4
  chuva(1, 5, 2, 149);
  tone(3, 311, 149); //Eb/D#4
  chuva(1, 2, 0, 149);
  tone(3, 349, 149); //F4
  delay(1931);
  tone(3, 349, 149); //F4
  chuva(2, 9, 7, 149); //ISSO SUBSTITUI O DELAY I N T E I R O QUE TAVA AQUI ANTES, FAZER OS LEDS DE 2 EM 2
  tone(3, 415, 149); //Ab/G#4
  chuva(2, 7, 5, 149);
  tone(3, 466, 149); //Bb/A#4
  chuva(2, 5, 2, 149);
  tone(3, 466, 446); //Bb/A#4
  chuva(2, 2, 0, 149); //O PADRAO VAI SER 149 FODASE, SE FOR MAIOR QUE 149, DIMINUI 149 E O RESULTADO FICA COMO DELAY ALI EM BAIXO
  delay(297);
  tone(3, 415, 297); //Ab/G#4
  chuva(2, 9, 0, 745);
  delay(740);
  tone(3, 349, 149); //F4
  chuva(2, 9, 7, 149);
  Serial.print("Score: ");
  Serial.println(score);
  tone(3, 415, 149); //Ab/G#4
  chuva(2, 7, 5, 149);
  tone(3, 466, 149); //Bb/A#4
  chuva(2, 5, 2, 149);
  tone(3, 466, 446); //Bb/A#4
  chuva(2, 2, 0, 298);
  tone(3, 415, 298); //Ab/G#4
  chuva(0, 9, 5, 298);
  tone(3, 349, 298); //F4
  chuva(0, 5, 2, 298);
  tone(3, 311, 149); //Eb/D#4
  chuva(0, 2, 0, 149);
  tone(3, 349, 149); //F4
  delay(595);
  tone(3, 466, 149); //Bb/A#4
  chuva(1, 9, 7, 149);
  tone(3, 415, 149); //Ab/G#4
  chuva(1, 7, 5, 149);
  Serial.print("Score: ");
  Serial.println(score);
  tone(3, 349, 149); //F4
  chuva(1, 5, 2, 149);
  tone(3, 311, 149); //Eb/D#4
  chuva(1, 2, 0, 149);
  tone(3, 349, 149); //F4
  delay(1931);
  tone(3, 349, 149); //F4
  chuva(2, 9, 7, 149); //ISSO SUBSTITUI O DELAY I N T E I R O QUE TAVA AQUI ANTES, FAZER OS LEDS DE 2 EM 2
  tone(3, 415, 149); //Ab/G#4
  chuva(2, 7, 5, 149);
  tone(3, 466, 149); //Bb/A#4
  chuva(2, 5, 2, 149);
  tone(3, 466, 446); //Bb/A#4
  chuva(2, 2, 0, 149); //O PADRAO VAI SER 149 FODASE, SE FOR MAIOR QUE 149, DIMINUI 149 E O RESULTADO FICA COMO DELAY ALI EM BAIXO
  delay(297);
  tone(3, 415, 297); //Ab/G#4
  chuva(2, 9, 0, 745);
  delay(740);
  tone(3, 349, 149); //F4
  chuva(2, 9, 7, 149);
  tone(3, 415, 149); //Ab/G#4
  chuva(2, 7, 5, 149);
  tone(3, 466, 149); //Bb/A#4
  chuva(2, 5, 2, 149);
  tone(3, 466, 446); //Bb/A#4
  chuva(2, 2, 0, 298);
  tone(3, 415, 298); //Ab/G#4
  chuva(0, 9, 5, 298);
  tone(3, 349, 298); //F4
  chuva(0, 5, 2, 298);
  tone(3, 311, 149); //Eb/D#4
  chuva(0, 2, 0, 149);
  Serial.print("Score: ");
  Serial.println(score);
  tone(3, 349, 149); //F4
  delay(595);
  tone(3, 466, 149); //Bb/A#4
  chuva(1, 9, 7, 149);
  tone(3, 415, 149); //Ab/G#4
  chuva(1, 7, 5, 149);
  tone(3, 349, 149); //F4
  chuva(1, 5, 2, 149);
  tone(3, 311, 149); //Eb/D#4
  chuva(1, 2, 0, 149);
  tone(3, 349, 149); //F4
  delay(1931);
  verifdmus = 1;
  highscoreread();
  finalstats();

}

void chuva(int col, int ini, int fim, int temp) {
  for (int loop = ini; loop >= fim; loop--) {
    digitalWrite(matrizV[col], HIGH);
    digitalWrite(matrizH[loop], LOW);

    if (digitalRead(botoes[0]) == HIGH || digitalRead(botoes[1]) == HIGH || digitalRead(botoes[2]) == HIGH || digitalRead(botoes[3]) == HIGH || digitalRead(botoes[4] == HIGH)) {
      contadordclicks++;
    }

    if (loop != ini) {
      digitalWrite(matrizH[loop + 1], HIGH);
    } //desligar o de tras tone 1

    if (loop == 3 || loop == 4) {
      if (digitalRead(botoes[col])) {
        valveanticheat = 1;
      } else {
        valveanticheat = 0;
      }
    }
    if (loop == 1) {
      if (digitalRead(botoes[col]) == HIGH && valveanticheat == 0) {
        contadornotasparaACERTARkkk++;
        chain++;
        scores();
        simounao = 1;
      }
    }else
    if (loop == 0) {
      contadornotasparaapertarkkk++;
      if (simounao == 0) {
        if (digitalRead(botoes[col]) == HIGH && valveanticheat == 0) {
          contadornotasparaACERTARkkk++;
          chain++;
          scores();
        } else {
          vida = vida - 1;
          chain = 0;
        }
      }
    }
    if (loop == 9) {
      simounao = 0;
    }
    escolha4 = Serial.read();
    if (escolha4 == 98) {
      Serial.println("");
      Serial.print("Voce cancelou a musica, voltando para o home...");
      noTone(3);
      delay(1000);
      resetFunc();
    }
    vidas();
    int c = temp / (ini - fim + 1);
    delay(c);
  }
  
  digitalWrite(matrizV[col], LOW);
  digitalWrite(matrizH[0], HIGH);
}
